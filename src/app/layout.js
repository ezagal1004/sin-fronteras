import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Conoce Tus Derechos - Asistente de Audio para Encuentros con ICE",
  description: "Aplicación móvil que ayuda a comunicar derechos constitucionales en inglés claro durante encuentros con ICE. Protege tus derechos con audio profesional en situaciones de emergencia.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}